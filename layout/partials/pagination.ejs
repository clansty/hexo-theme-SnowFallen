

    <!-- {{#if prev}}
    	<div class="prevpage"><a href="{{page_url prev}}" class="mif-chevron-thin-left mif-4x fg-white"></a></div>
    {{/if}}
    {{#if next}}
        <div class="nextpage"><a href="{{page_url next}}" class="mif-chevron-thin-right mif-4x fg-white"></a></div>
    {{/if}} -->
<p>
<% if(page.prev){ %>
    
        <a href="/<%- page.prev_link %>">上一页
        </a>
    
<% } %>

<% if(page.next){ %>
        <a href="/<%- page.next_link %>">下一页
        </a>
<% } %>
</p>